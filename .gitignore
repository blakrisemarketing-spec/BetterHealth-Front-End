node_modules/
dist/
.DS_Store
*.local
.env
.env.*
!.env.example

# Credentials — never commit
credentials.md
# SEO automation secrets (service-account keys, API key dumps)
*service-account*.json
*-sa.json
*.pem
*.key
seo/**/secrets*

# Allow hyperframes project dist (generated videos)
!hyperframes-project/dist/
!hyperframes-project/dist/**

# Allow GitHub Pages hyperframes gallery
!dist/hyperframes/
!dist/hyperframes/**
